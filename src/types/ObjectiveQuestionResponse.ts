export type ObjectiveQuestionResponse = {
    currentQuestionNumber: number;
    hasSubmitted:boolean;
    question: any;
    referenceId:string;
    responseId:string;
    totalQuestions:number;
  };
